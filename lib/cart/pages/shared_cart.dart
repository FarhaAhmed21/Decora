import 'package:flutter/material.dart';

class SharedCart extends StatelessWidget {
  const SharedCart({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("SharedCart"));
  }
}