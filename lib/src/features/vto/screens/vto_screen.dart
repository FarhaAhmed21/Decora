import 'package:flutter/material.dart';

class VtoScreen extends StatelessWidget {
  const VtoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
